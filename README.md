
  # HOw to get it to work locally for now

  ## Running the code

  Run `npm install` to install the dependencies.

  Run `npm run dev` to start the development server.
  
  Each file/page can be found under src -> components... This what everyone should see when they open our website